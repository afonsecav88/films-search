import { styled } from 'styled-components';

export const FooterCardItemTitle = styled.p`
  text-align: center;
  font-size: smaller;
  padding-right: 0.3rem;
  font-weight: bold;
  color: #4b515d;
`;
